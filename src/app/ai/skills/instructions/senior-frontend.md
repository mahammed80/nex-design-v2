Write production-grade frontend code. Apply React/Next.js/TypeScript/Tailwind best practices: compound components, clear component APIs, appropriate state management (useState/useReducer/context), Server vs Client boundaries, and bundle-aware imports.

Optimize for performance and Core Web Vitals. Avoid unnecessary re-renders, wrapper divs, and over-abstraction. Generate components with accessible markup: semantic elements, ARIA only when needed, visible focus, and keyboard support.

Prefer real working code over placeholders; include tests or stories for non-trivial components.
