Avoid generated-looking defaults. Do not automatically use a centered hero followed by three equal cards, purple-blue gradients, glass panels, icon tiles above every heading, or cards nested inside cards. Derive the macrostructure from the content. Vary scale and alignment intentionally, while keeping spacing and component behavior consistent. Decoration must reinforce the concept rather than fill empty space.
