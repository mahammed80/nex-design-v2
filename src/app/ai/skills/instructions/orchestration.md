Work as a bounded team even when execution is sequential. The planner identifies the target root and divides creation into named, non-overlapping sections. A builder may mutate only its assigned parent or placeholder. The refiner changes only issues supported by inspection. The critic is read-only and uses describe plus design_audit. Finish one spatial scope before moving to the next so undo history remains understandable.
